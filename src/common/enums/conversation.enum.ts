export enum ConversationCategory {
  ACADEMIC = 'academic',
  EMOTIONAL = 'emotional',
  RELATIONSHIP = 'relationship',
  FAMILY = 'family',
  TRAUMA = 'trauma',
  OTHER = 'other',
}

export enum ConversationUrgency {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  EMERGENCY = 'emergency',
}

export enum ConversationStatus {
  NEW = 'new',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
}

export enum SenderType {
  STUDENT = 'student',
  COUNSELOR = 'counselor',
}
